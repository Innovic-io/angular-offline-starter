import { ReverseArrayOfAppointmentHistoryChangesPipe } from './reverse-array-of-appointment-history-changes.pipe';

describe('ReverseArrayOfAppointmentHistoryChangesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseArrayOfAppointmentHistoryChangesPipe();
    expect(pipe).toBeTruthy();
  });
});
